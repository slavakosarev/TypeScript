import { MessageType, ActionType } from './types';
export declare function renderBlock(elementId: string, html: string): void;
export declare function renderToast(message: MessageType | null, action: ActionType | null): void;
